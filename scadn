#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
if [[ -x "${SCRIPT_DIR}/docbatchscan.sh" ]]; then
  exec "${SCRIPT_DIR}/docbatchscan.sh" "$@"
fi

exec docbatchscan "$@"
